{
  "name": "@ultreon/quantum-main",
  "version": "1.0.0",
  "description": "QuantumVoxel",
  "main": "index.mjs",
  "scripts": {
    "build": "rm -rf ../../dist && tsc -p tsconfig.json",
    "package": "npm run build && cp ./package.json ./dist && npm pack ./dist --workspace ."
  },
  "author": "XyperCode",
  "license": "Apache-2.0",
  "dependencies": {
    "@ultreon/quantum-stubs": "file:../quantum-stubs/quantumjs.tgz"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
